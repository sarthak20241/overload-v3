
# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
# @end expo-cli

# Dependencies
node_modules/

# Build artifacts
dist/
build/
.expo/
web-build/

# Native — generated by `expo prebuild`, not source of truth
android/
ios/
*.hbc

# Env / secrets
.env
.env.local
.env.*.local

# EAS submit credentials — Google Play service account key
google-service-account.json
*.p8
AuthKey_*.p8

# Migration tooling — secrets and any user-data exports (node_modules already ignored above)
scripts/migration/.env.migration
scripts/migration/*.export.json

# OS / editor
.DS_Store
.vscode/
.idea/
*.log

# Reference-only external project (should not live in repo)
Redesign Gym Workout Tracker UI*/

# Tooling temp / local working dirs (never upload to EAS)
.claude/worktrees/
supabase/.temp/
