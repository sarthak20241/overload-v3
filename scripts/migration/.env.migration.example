# Copy to .env.migration (gitignored) and fill in. NEVER commit the real file.
#
# Source: legacy circle-rate MongoDB Atlas cluster.
# Password must be URL-encoded (e.g. @ -> %40).
MONGO_URI="mongodb+srv://USER:PASSWORD@circle-rate-dev.ufngu8d.mongodb.net/?retryWrites=true&w=majority"

# Comma-separated identifiers for the users being migrated (matched by email/username).
MIGRATE_USERS="rana.sankalp99@gmail.com,tushar19212@iiitd.ac.in"
